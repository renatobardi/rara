name: CI rara-distill

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'rara-distill/**'
      - '.github/workflows/ci-distill.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'rara-distill/**'
      - '.github/workflows/ci-distill.yml'

jobs:
  code-quality:
    name: Code Quality & Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: rara-distill/go.mod

      - name: Cache Go modules
        uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-distill-${{ hashFiles('rara-distill/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-distill-

      - name: Download dependencies
        run: |
          cd rara-distill
          go mod download

      - name: Format check
        run: |
          cd rara-distill
          if [ -n "$(gofmt -s -l .)" ]; then
            echo "❌ Code formatting issues found. Run: gofmt -w ."
            gofmt -s -d .
            exit 1
          fi
          echo "✅ Code format OK"

      - name: Lint
        run: |
          cd rara-distill
          go vet ./...
          echo "✅ Vet check passed"

      - name: Run tests
        run: |
          cd rara-distill
          go test -v -race -coverprofile=coverage.out ./...
          echo "✅ Tests passed"

      - name: Check coverage
        run: |
          cd rara-distill
          go tool cover -func=coverage.out | tail -1
          echo "✅ Coverage report generated"

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: rara-distill/go.mod

      - name: Vulnerability scan (govulncheck)
        run: |
          cd rara-distill
          go install golang.org/x/vuln/cmd/govulncheck@latest
          "$(go env GOPATH)/bin/govulncheck" ./...

      - name: Secret scan (gitleaks)
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker-build:
    name: Docker build (multi-module)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      # Build only, no push — proves the multi-module Dockerfile compiles on every PR
      # without GCP credentials. Context is the repo root so `replace ../rara-addon`
      # resolves. See DOCKER-MULTIMODULE.md.
      - name: Build image
        run: docker build -f rara-distill/Dockerfile .
