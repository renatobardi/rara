name: Deploy rara-distill

# GATED — manual dispatch only (NO push:main). Thin caller of the reusable multi-module deploy
# (_reusable-deploy-worker.yml). rara-distill is a 2.0 bridge-total claim-worker: it only does
# work once the reconciler (VPC, a future phase) routes `destilar` item_steps to its provider.
# Job name = CLOUD_RUN_JOB_PREFIX(rara-) + provider(distill) = rara-distill (what the Activator
# wakes). LITELLM_BASE_URL is the LiteLLM Cloud Run URL — set the repo var vars.LITELLM_BASE_URL
# after A2 (deploy LiteLLM); LITELLM_API_KEY reads the litellm-api-key secret.
on:
  workflow_dispatch:
    inputs:
      run_after_deploy:
        description: 'Execute the job once right after deploying (off by default — the worker needs its provider env set first)'
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/_reusable-deploy-worker.yml
    with:
      app: rara-distill
      job_name: rara-distill
      # NOTE: groq-llama hardcoded here (llama-3.3-70b) — distill needs 70b quality for curation chains.
      # vars.LITELLM_MODEL=gemini-flash is spend-capped → ignored. Edit this YAML to change the model.
      env_vars: DISTILL_PROVIDER=distill,LITELLM_BASE_URL=${{ vars.LITELLM_BASE_URL }},LITELLM_MODEL=groq-llama
      extra_secrets: LITELLM_API_KEY=litellm-api-key:latest
      run_after_deploy: ${{ inputs.run_after_deploy }}
    secrets:
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
